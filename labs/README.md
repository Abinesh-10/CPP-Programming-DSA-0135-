# Labs folder

This folder is for your lab Word documents (e.g., .doc and .docx files).

Upload your files into this folder (e.g., `labs/`) so they're organized in the repository.

Suggested next steps:

- Upload your Word documents using Git locally, GitHub Desktop, or the web UI into this folder.
- If you prefer to add them in a separate branch for a pull request, create a branch (for example `add-labs`) and push your files there.

If you want, I can:
- create a branch and add a placeholder commit with this README (I used the repository default branch), or
- upload files for you if you provide them here or grant a link to download them.
